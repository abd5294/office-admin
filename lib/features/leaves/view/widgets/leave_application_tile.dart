import 'package:flutter/material.dart';

class LeaveApplicationTile extends StatelessWidget {
  const LeaveApplicationTile({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: ,
    );
  }
}

class LeaveApplicationExpandedTile extends StatelessWidget {
  const LeaveApplicationExpandedTile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
